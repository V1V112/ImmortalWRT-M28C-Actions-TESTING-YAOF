# ============================================================
# 可选原始 Kconfig 覆盖区
# ============================================================
# 用户上传的 m28c.config 已经拆分到：
# - profiles/m28c/target.config
# - profiles/m28c/packages.txt
#
# 如果后续需要临时追加不能写成软件包名的原始 Kconfig 行，
# 可以写在这里；也可以在 GitHub Actions 的 extra_config 输入中追加。
